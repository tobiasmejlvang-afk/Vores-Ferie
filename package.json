{
  "name": "vores-ferie",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Privat ferieplanlægger til GitHub Pages",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node scripts/build.mjs && node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs"
  }
}
